Scribe backend (Speech-to-Text and drafting)

- Endpoints under `/api/scribe/*`.
- Prompts live in `app/scribe/prompts/*.md`.
- Uses shared helpers in `app/ai_tools` as needed.

Start with `/api/scribe/transcribe` (currently a stub returning 501).
